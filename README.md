# 🏛️ Глас — Умный гражданский ИИ-ассистент для городов

**Глас** — это интеллектуальная мультиплатформенная система для сбора, анализа и управления обращениями граждан. Система использует искусственный интеллект для понимания текстов, фото и голосовых сообщений, автоматически маршрутизирует их по ответственным отделам городской администрации и визуализирует результаты.

## 🎯 Миссия проекта

- **Прозрачность:** каждый житель видит, что его обращение не пропало, а действительно рассматривается и решается
- **Доступность:** простой интерфейс для любого возраста и уровня цифровой грамотности
- **Этичный ИИ:** никакой слежки, только помощь и аналитика, направленные на благо города
- **Эффективность:** городские службы получают структурированные данные, аналитические отчёты и приоритеты проблем

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Python 3.11+ (для локальной разработки)
- Node.js 18+ (для локальной разработки)

### Установка и запуск

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd Voice
```

2. **Создайте файл `.env` в корне проекта:**
```env
JWT_SECRET=your-super-secret-jwt-key-change-this
OPENAI_API_KEY=your-openai-api-key
MAP_API_KEY=your-map-api-key
```

3. **Запустите проект через Docker Compose:**
```bash
docker-compose up -d
```

4. **Откройте в браузере:**
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API Документация: http://localhost:8000/docs

### Первоначальная настройка

После первого запуска создайте администратора и инициализируйте департаменты:

```bash
# Создание администратора
docker-compose exec backend python scripts/create_admin.py

# Инициализация департаментов (опционально)
docker-compose exec backend python scripts/init_departments.py
```

**Учетные данные администратора по умолчанию:**
- Email: `admin@glas.ru`
- Password: `admin123`

⚠️ **ВАЖНО:** Смените пароль после первого входа!

## 📁 Структура проекта

```
Voice/
├── backend/              # FastAPI backend
│   ├── app/
│   │   ├── api/         # API endpoints
│   │   ├── core/        # Конфигурация, безопасность
│   │   ├── models/      # SQLAlchemy модели
│   │   ├── schemas/     # Pydantic схемы
│   │   ├── services/    # Бизнес-логика
│   │   └── ai/          # AI/NLP модули
│   ├── alembic/         # Миграции БД
│   └── tests/           # Тесты
├── frontend/            # Next.js frontend
│   ├── src/
│   │   ├── app/         # Next.js App Router
│   │   ├── components/  # React компоненты
│   │   ├── lib/         # Утилиты
│   │   └── hooks/       # React hooks
│   └── public/          # Статические файлы
├── docker-compose.yml   # Docker конфигурация
└── README.md            # Документация
```

## 🛠️ Технологический стек

### Backend
- **FastAPI** — современный Python веб-фреймворк
- **PostgreSQL** — основная база данных
- **Redis** — кэширование и очереди
- **SQLAlchemy** — ORM
- **Alembic** — миграции БД
- **Pydantic** — валидация данных
- **JWT** — аутентификация

### Frontend
- **Next.js 14** — React фреймворк с App Router
- **TypeScript** — типизация
- **TailwindCSS** — стилизация
- **React Query** — управление состоянием и кэширование
- **Leaflet/OpenStreetMap** — карты

### AI/NLP
- **OpenAI API** — классификация и анализ текста
- **spaCy** — обработка естественного языка
- **Transformers** — ML модели

## 📋 Основные возможности

### Для граждан
- ✅ Создание обращений (текст, фото, голос)
- ✅ Отслеживание статуса обращения
- ✅ Геолокация проблем на карте
- ✅ Уведомления о статусе
- ✅ Оценка качества решения

### Для администраторов
- ✅ Дашборд с аналитикой
- ✅ Управление обращениями
- ✅ Автоматическая классификация и маршрутизация
- ✅ Экспорт отчетов (CSV, PDF)
- ✅ Тепловые карты проблем
- ✅ Анализ трендов

## 🔐 Безопасность

- JWT токены для аутентификации
- Хеширование паролей (bcrypt)
- Валидация всех входных данных
- Защита от SQL-инъекций
- CORS настройки
- Rate limiting
- Анонимизация персональных данных
- Соответствие GDPR/ФЗ-152

## 📊 API Документация

После запуска проекта документация доступна по адресу:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 🧪 Тестирование

```bash
# Backend тесты
cd backend
pytest

# Frontend тесты
cd frontend
npm test
```

## 🚢 Деплой в продакшн

### Подготовка

1. Обновите переменные окружения в `.env`
2. Настройте SSL сертификаты
3. Настройте домены для frontend и backend

### Docker Compose (рекомендуется)

```bash
docker-compose -f docker-compose.prod.yml up -d
```

### Kubernetes

Конфигурации Kubernetes находятся в директории `k8s/`

## 📈 Мониторинг

- Логи доступны через `docker-compose logs`
- Метрики: встроенные в FastAPI
- Health checks: `/health` endpoint

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 📞 Контакты

Для вопросов и предложений создайте Issue в репозитории.

---

**Глас — это голос города.**  
AI, который слышит людей, помогает власти действовать и делает жизнь в городе лучше.
