# Быстрый старт проекта Глас

## 🚀 За 5 минут до запуска

### Шаг 1: Установка Docker

Убедитесь, что у вас установлен Docker и Docker Compose:
```bash
docker --version
docker-compose --version
```

### Шаг 2: Клонирование проекта

```bash
git clone <repository-url>
cd Voice
```

### Шаг 3: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Откройте `.env` и укажите:
- `JWT_SECRET` - любая случайная строка (минимум 32 символа)
- `OPENAI_API_KEY` - ваш ключ OpenAI (можно оставить пустым, будет работать без AI)

### Шаг 4: Запуск

```bash
docker-compose up -d
```

Подождите 1-2 минуты пока все сервисы запустятся.

### Шаг 5: Создание администратора

```bash
docker-compose exec backend python scripts/create_admin.py
```

### Шаг 6: Откройте в браузере

- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:8000
- **API Документация:** http://localhost:8000/docs

### Шаг 7: Вход в систему

Используйте созданные учетные данные:
- Email: `admin@glas.ru`
- Password: `admin123`

**⚠️ ВАЖНО:** Смените пароль после первого входа!

## ✅ Готово!

Теперь вы можете:
- Создавать обращения
- Просматривать статистику
- Использовать AI классификацию
- Управлять системой

## 🛠 Полезные команды

```bash
# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Очистка (удаление всех данных)
docker-compose down -v
```

## ❓ Проблемы?

Если что-то не работает:
1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что порты 3000 и 8000 свободны
3. Проверьте, что Docker запущен

## 📚 Дополнительная документация

- [README.md](README.md) - Полная документация
- [DEPLOYMENT.md](DEPLOYMENT.md) - Развертывание в продакшн
- [API_DOCUMENTATION.md](API_DOCUMENTATION.md) - Документация API

