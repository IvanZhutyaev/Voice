# Настройка файла .env

## 📍 Где создавать файл?

**Файл `.env` нужно создать в главном каталоге проекта `Voice/`** (там же, где находится `docker-compose.yml`)

```
Voice/
├── .env              ← Создайте здесь!
├── docker-compose.yml
├── README.md
├── backend/
└── frontend/
```

## 📝 Как создать файл?

### Вариант 1: Скопировать из примера (рекомендуется)

```bash
# В каталоге Voice/
cp .env.example .env
```

### Вариант 2: Создать вручную

Создайте файл `.env` в корне проекта и скопируйте содержимое ниже.

## 📄 Пример файла .env

```env
# ============================================
# ФАЙЛ ПЕРЕМЕННЫХ ОКРУЖЕНИЯ ДЛЯ ПРОЕКТА ГЛАС
# ============================================

# --------------------------------------------
# JWT (JSON Web Token) - ОБЯЗАТЕЛЬНО
# --------------------------------------------
# Сгенерируйте случайный ключ: openssl rand -hex 32
# Или используйте любой случайный набор символов (минимум 32 символа)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# Время жизни токена (7 дней = 604800 секунд)
JWT_EXPIRATION=604800

# --------------------------------------------
# OpenAI API - ОПЦИОНАЛЬНО
# --------------------------------------------
# Оставьте пустым, если нет ключа OpenAI
# AI функции будут работать в упрощенном режиме
OPENAI_API_KEY=

# --------------------------------------------
# Карты - ОПЦИОНАЛЬНО
# --------------------------------------------
# Оставьте пустым для использования OpenStreetMap (бесплатно)
MAP_API_KEY=

# --------------------------------------------
# База данных (для Docker Compose)
# --------------------------------------------
DATABASE_URL=postgresql://glas_user:glas_password@postgres:5432/glas_db

# --------------------------------------------
# Redis (для Docker Compose)
# --------------------------------------------
REDIS_URL=redis://redis:6379

# --------------------------------------------
# Окружение
# --------------------------------------------
ENVIRONMENT=development

# --------------------------------------------
# CORS
# --------------------------------------------
CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]
ALLOWED_HOSTS=["*"]

# --------------------------------------------
# Загрузка файлов
# --------------------------------------------
MAX_UPLOAD_SIZE=10485760
UPLOAD_DIR=uploads
```

## ⚙️ Минимальная настройка для запуска

Для быстрого старта достаточно изменить только `JWT_SECRET`:

```env
JWT_SECRET=my-super-secret-key-for-jwt-tokens-min-32-characters
DATABASE_URL=postgresql://glas_user:glas_password@postgres:5432/glas_db
REDIS_URL=redis://redis:6379
ENVIRONMENT=development
CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]
ALLOWED_HOSTS=["*"]
```

Остальные параметры можно оставить пустыми или использовать значения по умолчанию.

## 🔐 Генерация JWT_SECRET

### Windows (PowerShell):
```powershell
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
```

### Linux/Mac:
```bash
openssl rand -hex 32
```

Или просто используйте любую случайную строку длиной минимум 32 символа.

## ✅ Проверка

После создания файла `.env` проверьте:

1. Файл находится в корне проекта `Voice/`
2. Файл называется именно `.env` (с точкой в начале)
3. `JWT_SECRET` заполнен (минимум 32 символа)

## 🚀 После создания .env

Запустите проект:

```bash
docker-compose up -d
```

## ⚠️ Важно

- **НЕ коммитьте** файл `.env` в git (он уже в .gitignore)
- **НЕ делитесь** содержимым `.env` публично
- В продакшне используйте **сильные секретные ключи**

